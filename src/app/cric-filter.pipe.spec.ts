import { CricFilterPipe } from './cric-filter.pipe';

describe('CricFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CricFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
